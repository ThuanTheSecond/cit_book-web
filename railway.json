{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "apt-get install rsync && rsync -a --ignore-existing ./media/ /app/media_data/ && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn book_cit_web.wsgi --forwarded-allow-ips '*'",
    "restartPolicyType": "ON_FAILURE", 
    "restartPolicyMaxRetries": 10
  }
}
